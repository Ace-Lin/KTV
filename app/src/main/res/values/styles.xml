<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


            <!--TODO:选择图片底部弹窗-->
    <style name="BottomDialog" parent="@style/Base.V7.Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="BottomDialog.Animation" parent="Animation.AppCompat.Dialog">
        <item name="android:windowEnterAnimation">@anim/translate_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/translate_dialog_out</item>
    </style>

    <style name="transparentFrameWindowStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@drawable/photo_choose_bg</item>
    </style>

    <style name="main_menu_animstyle">
        <item name="android:windowEnterAnimation">@anim/photo_dialog_in_anim</item>
        <item name="android:windowExitAnimation">@anim/photo_dialog_out_anim</item>
    </style>

      <!--TODO:自定义dialog背景全透明无边框theme -->
    <style name="PayDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowEnterAnimation">@anim/scale_center_in</item>
        <item name="android:windowExitAnimation">@anim/scale_center_out</item>
    </style>

    <!--TODO:进度条加载 -->
    <style name="ProgressBarDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度，尽量这里设置，透明或者直接需要的背景不然圆角会出现底边-->
        <item name="android:windowBackground">@drawable/shape_circle_pad_black</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowEnterAnimation">@anim/scale_center_in</item>
        <item name="android:windowExitAnimation">@anim/scale_center_out</item>
        <!-- 背景不模糊，弹窗外视图没有黑色透明遮罩 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!--不要这里设置透明度，这是对所有控件进行透明度设置，如果只是设置背景透明度，代码获取设置-->
        <!--<item name="android:alpha">0.6</item>-->
    </style>

</resources>
